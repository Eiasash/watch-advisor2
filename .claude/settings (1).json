{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'if echo \"$CLAUDE_TOOL_OUTPUT\" | grep -q \"push origin main\" 2>/dev/null; then echo \"[skill-hook] Deploy detected — queuing skill update\"; fi'"
          }
        ]
      }
    ]
  },
  "postDeploy": {
    "commands": ["/update-skill"]
  }
}
