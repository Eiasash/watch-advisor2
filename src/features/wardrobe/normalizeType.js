/**
 * Re-exports normalizeType from the single canonical source.
 * All type normalization uses src/classifier/normalizeType.js.
 */
export { normalizeType } from "../../classifier/normalizeType.js";
