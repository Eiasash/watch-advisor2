/**
 * Re-exports from src/config/weatherRules.js — single source of truth.
 * All weather logic lives in config; import from there for new code.
 */
export { weatherLayerSuggestion, weatherDisplayText } from "../../config/weatherRules.js";
