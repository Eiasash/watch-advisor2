/* Per-app config for shared install-promo.js. Loaded BEFORE install-promo.js.
 * Strict CSP (sha-pinned inline) — keep config in its own file. */
window.PWA_INSTALL_CONFIG = { appName: 'Watch Advisor' };
